# TradeWinds (트레이드윈즈) - PRD (Product Requirements Document)

> **버전:** 1.0.0  
> **최종 수정일:** 2024년 12월  
> **작성자:** Product Team

---

## 목차

1. [Executive Summary](#1-executive-summary)
2. [서비스 브랜딩](#2-서비스-브랜딩-branding)
3. [UX/UI 컨셉 및 용어 정의](#3-uxui-컨셉-및-용어-정의)
4. [핵심 기능 명세](#4-핵심-기능-명세-core-features)
5. [시스템 아키텍처](#5-시스템-아키텍처)
6. [데이터베이스 설계](#6-데이터베이스-설계)
7. [마케팅 및 수익화 전략](#7-마케팅-및-수익화-전략)
8. [개발 로드맵](#8-개발-로드맵)

---

## 1. Executive Summary

### 1.1 프로젝트 개요

**TradeWinds**는 전 세계 145개 크라우드펀딩 사이트(Kickstarter, Indiegogo, Makuake 등)에서 발견되는 혁신적인 상품을 수집하여 사용자에게 큐레이션해주는 쇼핑몰 앱입니다.

### 1.2 핵심 차별점

| 요소 | 기존 서비스 | TradeWinds |
|------|------------|------------|
| 콘텐츠 소스 | 단일 플랫폼 | 145개 글로벌 펀딩 사이트 |
| 사용자 경험 | 일반적인 쇼핑몰 UI | 대항해시대 테마의 게이미피케이션 |
| 상품 발견 | 카테고리 검색 | 세계 지도 탐험 방식 |
| 리워드 | 포인트 적립 | 선장 등급 시스템, 금화 |

### 1.3 비전

> *"전 세계의 혁신을 발견하는 디지털 탐험가들의 항해"*

사용자가 단순히 쇼핑하는 것이 아니라, 전 세계를 탐험하며 미래의 트렌드를 먼저 발견하는 경험을 제공합니다.

### 1.4 기술 스택

- **Frontend:** Flutter (Android/iOS 크로스 플랫폼)
- **Backend:** Supabase (PostgreSQL, Authentication, Storage, Edge Functions)
- **Crawling:** Python (Scrapy/BeautifulSoup) + Supabase Edge Functions
- **State Management:** flutter_bloc
- **지도:** flutter_map + OpenStreetMap

---

## 2. 서비스 브랜딩 (Branding)

### 2.1 서비스 네이밍

#### 최종 선정: **TradeWinds (트레이드윈즈)**

| 후보 | 의미 | 장점 | 단점 |
|------|------|------|------|
| **TradeWinds** ✓ | 대항해시대 무역풍 | 글로벌, 세련됨, 앱스토어 최적화 | - |
| CrowdSailing | Crowdfunding + Sailing | 직관적 설명 | 다소 긴 이름 |
| Portfounder | Port + Founder | 창업/발견 뉘앙스 | 발음 어려움 |

**선정 이유:**
- "무역풍(Trade Winds)"은 대항해시대 범선의 항해를 가능하게 한 바람
- 글로벌 교역의 의미 내포
- 영문 앱스토어에서의 검색 최적화 용이
- 짧고 기억하기 쉬움

### 2.2 캐치프레이즈

#### 메인 슬로건
> **"미지의 보물을 찾아 떠나는 당신의 항해"**

#### 서브 슬로건
> *"전 세계 145개 항구에서 발견한 내일의 트렌드"*

#### 상황별 카피

| 상황 | 카피 |
|------|------|
| 앱 스플래시 | "새로운 항해를 시작합니다" |
| 온보딩 | "당신만의 무역선에 오신 것을 환영합니다, 선장님" |
| 상품 발견 시 | "새로운 보물을 발견했습니다!" |
| 구매 완료 | "교역 성공! 보물이 본국으로 향합니다" |
| 빈 장바구니 | "선적 화물이 비어있습니다. 보물을 찾아 떠나볼까요?" |

### 2.3 세계관 설정

```
═══════════════════════════════════════════════════════════════
                    『 TradeWinds 세계관 』
═══════════════════════════════════════════════════════════════

▶ 시대 배경
  21세기, 디지털 대항해시대가 열렸습니다.
  
▶ 세계 설정
  전 세계 145개의 항구(크라우드펀딩 사이트)에서
  아직 세상에 알려지지 않은 혁신적인 보물(얼리어답터 제품)들이
  매일 발견되고 있습니다.

▶ 당신의 역할
  당신은 이 보물들을 발견하고 교역하는 탐험가 선장입니다.
  "TradeWinds"는 당신만의 무역선입니다.
  
▶ 미션
  - 세계 지도를 펼치고 항구를 선택하세요
  - 숨겨진 보물을 발견하세요
  - 발견한 보물을 선적하여 본국으로 운송하세요
  - 항해 경험을 쌓아 선장 등급을 올리세요
  
▶ 보상
  - 항해 거리에 따라 경험치(XP) 획득
  - 금화 적립으로 할인 혜택
  - 선장 등급 상승으로 특별 혜택 잠금 해제

═══════════════════════════════════════════════════════════════
```

### 2.4 선장 등급 시스템

| 등급 | 영문명 | 필요 XP | 혜택 |
|------|--------|---------|------|
| 🔰 견습생 | Apprentice | 0 | 기본 기능 |
| ⚓ 항해사 | Navigator | 1,000 | 주간 보물 알림 |
| 🚢 선장 | Captain | 5,000 | 신규 보물 선공개 |
| 👑 제독 | Admiral | 20,000 | 전용 할인, 독점 항로 |
| 🏴‍☠️ 전설의 선장 | Legend | 100,000 | VIP 컨시어지 서비스 |

---

## 3. UX/UI 컨셉 및 용어 정의

### 3.1 용어 매핑 테이블

쇼핑몰의 일반적인 용어를 대항해시대 컨셉에 맞게 재정의합니다.

| 일반 용어 | TradeWinds 용어 | 아이콘 | 설명 |
|-----------|----------------|--------|------|
| 홈 화면 | 항해 본부 | 🏠 | 메인 대시보드 |
| 카테고리 | 항로 | 🧭 | 상품 분류 체계 |
| 검색 | 정찰 | 🔍 | 상품 검색 기능 |
| 상품 목록 | 보물 목록 | 📜 | 크롤링된 상품 리스트 |
| 상품 상세 | 보물 감정서 | 📋 | 상품 상세 페이지 |
| 장바구니 | 선적 화물 | 📦 | 구매 예정 상품 |
| 찜하기/위시리스트 | 보물 지도 | 🗺️ | 관심 상품 저장 |
| 최근 본 상품 | 항해 일지 | 📖 | 브라우징 히스토리 |
| 마이페이지 | 선장실 | 🚪 | 개인 설정 및 정보 |
| 알림 | 봉화 신호 | 🔔 | 푸시 알림 |
| 리뷰/평점 | 항해사 평가 | ⭐ | 상품 리뷰 |
| 회원 등급 | 선장 계급 | 🎖️ | 등급 시스템 |
| 포인트/적립금 | 금화 | 🪙 | 리워드 시스템 |
| 쿠폰 | 항해 허가증 | 🎫 | 할인 쿠폰 |
| 구매 완료 | 교역 성공 | ✅ | 결제 완료 상태 |
| 배송 중 | 귀항 중 | 🚢 | 배송 진행 상태 |
| 배송 완료 | 입항 완료 | ⚓ | 배송 완료 상태 |
| 펀딩 사이트 | 항구 | 🏴 | 크라우드펀딩 플랫폼 |
| 국가/지역 | 대륙/해역 | 🌍 | 지역 분류 |

### 3.2 항로(카테고리) 분류

| 항로명 | 아이콘 | 포함 카테고리 |
|--------|--------|---------------|
| 테크놀로지 항로 | 💻 | 가젯, 전자기기, IoT |
| 라이프스타일 항로 | 🏠 | 홈/리빙, 인테리어 |
| 패션 항로 | 👔 | 의류, 액세서리, 시계 |
| 아웃도어 항로 | ⛺ | 캠핑, 여행, 스포츠 |
| 크리에이티브 항로 | 🎨 | 아트, 디자인, 게임 |
| 푸드 & 드링크 항로 | 🍷 | 식품, 음료, 주방용품 |
| 헬스케어 항로 | 💪 | 건강, 피트니스, 뷰티 |
| 에코 항로 | 🌱 | 친환경, 지속가능 제품 |

### 3.3 컬러 팔레트

```dart
/// TradeWinds 앱 컬러 시스템
/// 대항해시대의 모험적이고 신비로운 분위기를 표현

class AppColors {
  // ═══════════════════════════════════════════════════════
  // Primary Colors - 심해 블루 (신뢰, 모험, 깊이)
  // ═══════════════════════════════════════════════════════
  static const Color primary = Color(0xFF1A365D);       // Deep Navy
  static const Color primaryLight = Color(0xFF2C5282); // Ocean Blue
  static const Color primaryDark = Color(0xFF0F2942);  // Midnight Blue
  
  // ═══════════════════════════════════════════════════════
  // Secondary Colors - 골드 (보물, 가치, 성취)
  // ═══════════════════════════════════════════════════════
  static const Color gold = Color(0xFFD69E2E);         // Treasure Gold
  static const Color goldLight = Color(0xFFF6E05E);    // Sunlight Gold
  static const Color goldDark = Color(0xFFB7791F);     // Antique Gold
  
  // ═══════════════════════════════════════════════════════
  // Accent Colors - 코럴 (발견, 흥분, 알림)
  // ═══════════════════════════════════════════════════════
  static const Color coral = Color(0xFFE53E3E);        // Discovery Red
  static const Color coralLight = Color(0xFFFC8181);   // Sunset Coral
  
  // ═══════════════════════════════════════════════════════
  // Background Colors - 파치먼트 (고지도, 빈티지)
  // ═══════════════════════════════════════════════════════
  static const Color parchment = Color(0xFFFAF5E4);      // Old Paper
  static const Color parchmentDark = Color(0xFFE8DCC4);  // Aged Parchment
  static const Color parchmentDeep = Color(0xFFD4C4A8);  // Antique Map
  
  // ═══════════════════════════════════════════════════════
  // Neutral Colors - 목재/밧줄 (자연, 항해)
  // ═══════════════════════════════════════════════════════
  static const Color wood = Color(0xFF5D4E37);         // Ship Wood
  static const Color woodLight = Color(0xFF8B7355);    // Rope Brown
  static const Color woodDark = Color(0xFF3D3225);     // Dark Timber
  
  // ═══════════════════════════════════════════════════════
  // Semantic Colors
  // ═══════════════════════════════════════════════════════
  static const Color success = Color(0xFF38A169);      // 교역 성공
  static const Color warning = Color(0xFFDD6B20);      // 마감 임박
  static const Color error = Color(0xFFE53E3E);        // 오류/실패
  static const Color info = Color(0xFF3182CE);         // 정보
  
  // ═══════════════════════════════════════════════════════
  // Text Colors
  // ═══════════════════════════════════════════════════════
  static const Color textPrimary = Color(0xFF1A202C);
  static const Color textSecondary = Color(0xFF4A5568);
  static const Color textTertiary = Color(0xFF718096);
  static const Color textOnPrimary = Color(0xFFFFFFFF);
  static const Color textOnGold = Color(0xFF1A202C);
}
```

### 3.4 디자인 무드 & 스타일 가이드

#### 전체 무드
- **빈티지 해도(Nautical Chart) 스타일**: 배경에 고지도 텍스처 적용
- **브라스/골드 UI 요소**: CTA 버튼, 아이콘에 황동 느낌의 그라데이션
- **손글씨 폰트**: 제목과 강조 텍스트에 캘리그래피 스타일
- **나침반 모티브**: 하단 네비게이션, 로딩 인디케이터에 활용

#### Typography

```dart
class AppTypography {
  // Display - 서비스명, 주요 타이틀
  static const String displayFont = 'Playfair Display';
  
  // Heading - 섹션 제목, 강조
  static const String headingFont = 'Cinzel';
  
  // Body - 본문 텍스트
  static const String bodyFont = 'Noto Sans KR';
  
  // Accent - 특별 강조, 라벨
  static const String accentFont = 'Cormorant Garamond';
}
```

#### UI 컴포넌트 스타일

| 컴포넌트 | 스타일 | 특징 |
|----------|--------|------|
| 카드 | 파치먼트 배경 + 그림자 | 낡은 종이 느낌의 테두리 |
| 버튼 (Primary) | 골드 그라데이션 | 브라스 버튼 느낌 |
| 버튼 (Secondary) | 네이비 아웃라인 | 해군 제복 버튼 느낌 |
| 아이콘 | 골드/브라운 톤 | 황동 장식 느낌 |
| 구분선 | 밧줄 패턴 | 선박 밧줄 모티브 |
| 로딩 | 나침반 회전 | 방향을 찾는 느낌 |

### 3.5 화면 구성 (Information Architecture)

```
TradeWinds App
│
├── 🏠 항해 본부 (Home)
│   ├── 오늘 발견된 유물 (신규 상품)
│   ├── 떠오르는 보물 (인기 급상승)
│   ├── 선장들의 선택 (베스트)
│   └── 마감 임박 항해 (펀딩 종료 임박)
│
├── 🗺️ 세계 지도 (Explore)
│   ├── 인터랙티브 월드맵
│   ├── 대륙/국가 선택
│   └── 항구(사이트) 목록
│
├── 🧭 항로 (Categories)
│   ├── 테크놀로지 항로
│   ├── 라이프스타일 항로
│   └── ... (8개 카테고리)
│
├── 📦 선적 화물 (Cart)
│   ├── 선적 목록
│   ├── 예상 운송비
│   └── 교역 진행 (결제)
│
└── 🚪 선장실 (My Page)
    ├── 선장 프로필 & 등급
    ├── 보물 지도 (위시리스트)
    ├── 항해 일지 (주문 내역)
    ├── 금화 & 항해 허가증
    ├── 봉화 신호 설정
    └── 설정
```

---

## 4. 핵심 기능 명세 (Core Features)

### 4.1 세계 지도 탐험 (World Map Explorer)

#### 4.1.1 기능 개요

사용자가 인터랙티브한 세계 지도를 통해 국가별 크라우드펀딩 사이트를 탐험하고, 각 사이트의 상품을 브라우징하는 핵심 기능입니다.

```
┌─────────────────────────────────────────────────────────────┐
│                     세계 지도 탐험                            │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│        🌍 인터랙티브 세계 지도                                │
│                                                             │
│   [북미]           [유럽]           [아시아]                  │
│    🏴(32)           🏴(45)           🏴(38)                  │
│                                                             │
│   [남미]           [아프리카]        [오세아니아]              │
│    🏴(12)           🏴(8)            🏴(10)                  │
│                                                             │
├─────────────────────────────────────────────────────────────┤
│ [항구 선택 시 바텀시트]                                       │
│                                                             │
│  🇺🇸 미국 항구 (32개 사이트)                                  │
│  ├── Kickstarter        [1,234개 보물]  →                   │
│  ├── Indiegogo         [892개 보물]   →                    │
│  ├── GoFundMe          [456개 보물]   →                    │
│  └── ...더보기                                               │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

#### 4.1.2 기능 상세

| 기능 | 설명 | 구현 방식 |
|------|------|----------|
| 지도 표시 | 세계 지도 렌더링 | flutter_map + OpenStreetMap |
| 핀(항구) 마커 | 국가별 펀딩 사이트 수 표시 | Custom Marker Widget |
| 확대/축소 | 핀치 줌 제스처 | flutter_map 기본 기능 |
| 대륙 필터 | 대륙별 빠른 이동 | 상단 탭 바 |
| 바텀시트 | 국가 선택 시 사이트 목록 | DraggableScrollableSheet |
| 사이트 진입 | 사이트 선택 시 상품 목록 | Navigator push |

#### 4.1.3 지역 분류

| 대륙 | 주요 국가 | 대표 사이트 |
|------|----------|------------|
| 북미 | 미국, 캐나다 | Kickstarter, Indiegogo |
| 유럽 | 영국, 독일, 프랑스 | Crowdcube, Ulule, KissKissBankBank |
| 아시아 | 일본, 한국, 중국 | Makuake, Wadiz, JD Crowdfunding |
| 오세아니아 | 호주, 뉴질랜드 | Pozible, PledgeMe |
| 남미 | 브라질, 멕시코 | Catarse, Fondeadora |
| 중동/아프리카 | UAE, 남아공 | Eureeca, Thundafund |

### 4.2 보물 발견 시스템 (Product Discovery)

#### 4.2.1 홈 화면 섹션 구성

| 섹션명 | 대항해시대 명칭 | 선정 기준 | 업데이트 |
|--------|----------------|----------|----------|
| 신규 상품 | 오늘 발견된 유물 | 24시간 내 크롤링 | 실시간 |
| 인기 급상승 | 떠오르는 보물 | 펀딩률 상승폭 상위 | 6시간 |
| 베스트 | 선장들의 선택 | 찜 수 상위 | 일 1회 |
| 종료 임박 | 마감 임박 항해 | D-7 이내 | 일 2회 |
| AI 추천 | 숨겨진 보물 | 사용자 취향 기반 | 사용자별 |
| 에디터 픽 | 선장의 추천 | 관리자 수동 선정 | 주 1회 |

#### 4.2.2 보물(상품) 카드 UI

```
┌─────────────────────────────────────┐
│  [상품 이미지]                        │
│                                     │
│  🏴 Kickstarter  |  🇺🇸 미국          │
├─────────────────────────────────────┤
│  [상품명 - 2줄 제한]                  │
│  Smart Watch with AI...             │
│                                     │
│  💰 $125 (~₩167,000)                │
│                                     │
│  [펀딩 진행률 바 ████████░░ 82%]      │
│                                     │
│  ⏰ D-12 남음  |  👥 1,234명 참여      │
│                                     │
│  [🗺️ 보물지도 담기]  [📦 선적하기]      │
└─────────────────────────────────────┘
```

#### 4.2.3 보물 감정서 (상품 상세)

```
┌─────────────────────────────────────────────────────┐
│ [이미지 캐러셀]                                       │
│                                                     │
│ ← [1] [2] [3] [4] →                                 │
├─────────────────────────────────────────────────────┤
│                                                     │
│ 🏴 Kickstarter  →  원본 페이지 바로가기              │
│                                                     │
│ [상품명]                                            │
│ Revolutionary Smart Backpack with Solar Charger    │
│                                                     │
│ ─────────────────────────────────────────────────── │
│ 💰 교역가                                           │
│    $149 (~₩199,000)                                │
│    배송비: $25 (한국 기준)                           │
│                                                     │
│ ─────────────────────────────────────────────────── │
│ 📊 펀딩 현황                                        │
│    목표: $50,000                                   │
│    달성: $127,500 (255%)                           │
│    참여: 1,847명                                   │
│    남은 기간: D-15                                  │
│                                                     │
│ ─────────────────────────────────────────────────── │
│ 📝 보물 설명                                        │
│    [상세 설명 텍스트...]                             │
│    ...더보기                                        │
│                                                     │
│ ─────────────────────────────────────────────────── │
│ ⭐ 항해사 평가 (앱 내 리뷰)                           │
│    4.5 / 5.0 (23개 평가)                           │
│    [리뷰 목록]                                      │
│                                                     │
├─────────────────────────────────────────────────────┤
│ [🗺️ 보물지도 담기]        [📦 선적하기 - $149]       │
└─────────────────────────────────────────────────────┘
```

### 4.3 정찰 시스템 (Search)

#### 4.3.1 검색 기능

| 기능 | 설명 |
|------|------|
| 키워드 검색 | 상품명, 설명 텍스트 검색 |
| 자동완성 | 인기 검색어 기반 추천 |
| 검색 필터 | 가격대, 펀딩률, 남은 기간, 항구(사이트) |
| 정렬 옵션 | 최신순, 인기순, 펀딩률순, 마감임박순 |
| 검색 히스토리 | 최근 검색어 저장 |

#### 4.3.2 필터 옵션

```dart
class SearchFilters {
  // 가격대
  RangeValues priceRange; // $0 ~ $10,000+
  
  // 펀딩 진행률
  List<FundingStatus> fundingStatus; // 진행중, 목표달성, 종료
  int? minFundingPercent; // 최소 펀딩률
  
  // 기간
  DateRange? dateRange; // 등록일 기준
  int? daysUntilEnd; // 종료까지 남은 일수
  
  // 출처
  List<String> selectedPorts; // 선택된 항구(사이트)
  List<String> selectedRegions; // 선택된 지역
  
  // 카테고리
  List<String> categories; // 선택된 항로
}
```

### 4.4 선장실 (My Page)

#### 4.4.1 구성 요소

```
┌─────────────────────────────────────────────────────┐
│                      선장실                          │
├─────────────────────────────────────────────────────┤
│                                                     │
│   [아바타 이미지]                                    │
│   🚢 Captain_Kim                                   │
│   ⚓ 선장 (Captain) 등급                            │
│                                                     │
│   XP: 7,250 / 20,000 [████████░░░░] 다음: 제독     │
│                                                     │
│   🪙 보유 금화: 15,200                              │
│   🎫 항해 허가증: 3장                               │
│                                                     │
├─────────────────────────────────────────────────────┤
│                                                     │
│   📊 항해 통계                                       │
│   ├── 총 항해 횟수: 127회                           │
│   ├── 발견한 보물: 342개                            │
│   ├── 교역 성공: 23건                               │
│   └── 방문한 항구: 45개                             │
│                                                     │
├─────────────────────────────────────────────────────┤
│                                                     │
│   [🗺️ 보물 지도]           [📖 항해 일지]            │
│   [📦 교역 내역]           [🔔 봉화 신호 설정]        │
│   [🪙 금화 & 허가증]        [⚙️ 설정]                │
│                                                     │
└─────────────────────────────────────────────────────┘
```

#### 4.4.2 XP 획득 조건

| 활동 | 획득 XP | 일일 한도 |
|------|---------|----------|
| 앱 접속 | +10 | 1회 |
| 보물 상세 조회 | +5 | 50회 |
| 보물 지도 추가 | +15 | 20회 |
| 교역 성공 (구매) | +100 | 무제한 |
| 항해사 평가 작성 | +30 | 5회 |
| SNS 공유 | +20 | 3회 |
| 신규 항구 방문 | +50 | 무제한 |

### 4.5 봉화 신호 (Notifications)

#### 4.5.1 알림 유형

| 알림 유형 | 대항해시대 명칭 | 설명 |
|----------|----------------|------|
| 찜 상품 업데이트 | 보물 지도 속보 | 찜한 상품의 펀딩률/가격 변동 |
| 마감 임박 | 항해 종료 임박 | 찜한 상품 D-3, D-1 알림 |
| 신규 발견 | 새 보물 발견 | 관심 카테고리 신상품 |
| 금화/쿠폰 | 금화 적립, 허가증 발급 | 리워드 관련 |
| 배송 | 귀항 소식 | 배송 상태 변경 |
| 마케팅 | 선장 공지 | 이벤트, 프로모션 |

---

## 5. 시스템 아키텍처

### 5.1 전체 아키텍처

```
┌─────────────────────────────────────────────────────────────────┐
│                        TradeWinds System Architecture           │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │                    Flutter App (Client)                  │   │
│  │  ┌─────────┐  ┌─────────┐  ┌─────────┐  ┌─────────┐    │   │
│  │  │   UI    │──│  BLoC   │──│  Repo   │──│ Service │    │   │
│  │  │  Layer  │  │ (State) │  │ (Data)  │  │ (API)   │    │   │
│  │  └─────────┘  └─────────┘  └─────────┘  └─────────┘    │   │
│  └─────────────────────────────────────────────────────────┘   │
│                              │                                  │
│                              ▼                                  │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │                      Supabase (Backend)                  │   │
│  │  ┌───────────┐  ┌───────────┐  ┌───────────────────┐   │   │
│  │  │   Auth    │  │  Storage  │  │    PostgreSQL     │   │   │
│  │  │ (OAuth)   │  │  (Images) │  │   (Main DB)       │   │   │
│  │  └───────────┘  └───────────┘  └───────────────────┘   │   │
│  │                                         │               │   │
│  │  ┌───────────┐  ┌───────────┐          │               │   │
│  │  │   Edge    │  │  Realtime │          │               │   │
│  │  │ Functions │  │ (WebSocket)│         │               │   │
│  │  └───────────┘  └───────────┘          │               │   │
│  └─────────────────────────────────────────────────────────┘   │
│                              ▲                                  │
│                              │                                  │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │                   Crawling Server                        │   │
│  │  ┌───────────┐  ┌───────────┐  ┌───────────┐           │   │
│  │  │ Scheduler │──│  Scrapy   │──│  Parser   │──→ DB     │   │
│  │  │  (Cron)   │  │ (Spider)  │  │ (Clean)   │           │   │
│  │  └───────────┘  └───────────┘  └───────────┘           │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 5.2 Flutter 앱 구조

```
lib/
├── main.dart
├── app.dart
│
├── core/
│   ├── constants/
│   │   ├── app_colors.dart
│   │   ├── app_typography.dart
│   │   ├── app_strings.dart        # 대항해시대 용어
│   │   └── api_endpoints.dart
│   ├── theme/
│   │   └── app_theme.dart
│   ├── utils/
│   │   ├── extensions.dart
│   │   └── helpers.dart
│   └── network/
│       └── supabase_client.dart
│
├── data/
│   ├── models/
│   │   ├── treasure.dart           # 상품
│   │   ├── port.dart               # 펀딩 사이트
│   │   ├── captain.dart            # 사용자
│   │   ├── cargo.dart              # 장바구니
│   │   └── voyage_log.dart         # 조회 기록
│   ├── repositories/
│   │   ├── treasure_repository.dart
│   │   ├── port_repository.dart
│   │   ├── captain_repository.dart
│   │   └── auth_repository.dart
│   └── datasources/
│       ├── remote/
│       │   └── supabase_datasource.dart
│       └── local/
│           └── hive_datasource.dart
│
├── domain/
│   ├── entities/
│   └── usecases/
│       ├── get_treasures_usecase.dart
│       ├── search_treasures_usecase.dart
│       └── add_to_cargo_usecase.dart
│
├── presentation/
│   ├── blocs/
│   │   ├── auth/
│   │   ├── treasure/
│   │   ├── map/
│   │   ├── cart/
│   │   └── profile/
│   ├── pages/
│   │   ├── splash/
│   │   ├── onboarding/
│   │   ├── home/                   # 항해 본부
│   │   ├── map/                    # 세계 지도
│   │   ├── category/               # 항로
│   │   ├── search/                 # 정찰
│   │   ├── treasure_detail/        # 보물 감정서
│   │   ├── cart/                   # 선적 화물
│   │   └── profile/                # 선장실
│   └── widgets/
│       ├── common/
│       ├── treasure_card.dart
│       ├── port_marker.dart
│       └── compass_loading.dart
│
└── routes/
    └── app_router.dart
```

### 5.3 상태 관리 (BLoC Pattern)

```dart
// 예시: TreasureBloc
abstract class TreasureEvent {}
class LoadTreasures extends TreasureEvent {}
class LoadTreasuresByPort extends TreasureEvent {
  final String portId;
}
class SearchTreasures extends TreasureEvent {
  final String query;
  final SearchFilters? filters;
}

abstract class TreasureState {}
class TreasureInitial extends TreasureState {}
class TreasureLoading extends TreasureState {}
class TreasureLoaded extends TreasureState {
  final List<Treasure> treasures;
}
class TreasureError extends TreasureState {
  final String message;
}
```

---

## 6. 데이터베이스 설계

### 6.1 ERD (Entity Relationship Diagram)

```
┌─────────────────┐       ┌─────────────────┐
│     ports       │       │    treasures    │
│ (항구/펀딩사이트)│       │    (보물/상품)   │
├─────────────────┤       ├─────────────────┤
│ id (PK)         │──┐    │ id (PK)         │
│ name            │  │    │ port_id (FK)    │←─┘
│ country         │  └───→│ external_id     │
│ region          │       │ title           │
│ base_url        │       │ description     │
│ logo_url        │       │ image_urls      │
│ is_active       │       │ original_url    │
│ created_at      │       │ funding_goal    │
└─────────────────┘       │ funding_current │
                          │ funding_percent │
┌─────────────────┐       │ currency        │
│    captains     │       │ end_date        │
│  (선장/사용자)   │       │ category        │
├─────────────────┤       │ tags            │
│ id (PK/FK auth) │       │ crawled_at      │
│ nickname        │       │ is_featured     │
│ rank            │       └─────────────────┘
│ gold_coins      │              │
│ total_voyages   │              │
│ xp              │              │
│ avatar_url      │              │
│ created_at      │              │
└─────────────────┘              │
        │                        │
        │    ┌───────────────────┼───────────────────┐
        │    │                   │                   │
        ▼    ▼                   ▼                   ▼
┌─────────────────┐   ┌─────────────────┐   ┌─────────────────┐
│     cargo       │   │  treasure_maps  │   │   voyage_logs   │
│  (선적 화물)     │   │  (보물 지도)    │   │   (항해 일지)    │
├─────────────────┤   ├─────────────────┤   ├─────────────────┤
│ id (PK)         │   │ id (PK)         │   │ id (PK)         │
│ captain_id (FK) │   │ captain_id (FK) │   │ captain_id (FK) │
│ treasure_id (FK)│   │ treasure_id (FK)│   │ treasure_id (FK)│
│ quantity        │   │ created_at      │   │ viewed_at       │
│ added_at        │   └─────────────────┘   └─────────────────┘
└─────────────────┘
```

### 6.2 테이블 정의 (SQL)

```sql
-- ═══════════════════════════════════════════════════════════════
-- TradeWinds Database Schema
-- ═══════════════════════════════════════════════════════════════

-- Enable UUID extension
CREATE EXTENSION IF NOT EXISTS "uuid-ossp";

-- ───────────────────────────────────────────────────────────────
-- 1. ports (항구 - 크라우드펀딩 사이트)
-- ───────────────────────────────────────────────────────────────
CREATE TABLE ports (
    id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
    name VARCHAR(100) NOT NULL,
    country VARCHAR(50) NOT NULL,
    country_code VARCHAR(3) NOT NULL,
    region VARCHAR(50) NOT NULL,
    base_url VARCHAR(500) NOT NULL,
    logo_url VARCHAR(500),
    description TEXT,
    is_active BOOLEAN DEFAULT true,
    treasure_count INTEGER DEFAULT 0,
    last_crawled_at TIMESTAMPTZ,
    created_at TIMESTAMPTZ DEFAULT NOW(),
    updated_at TIMESTAMPTZ DEFAULT NOW()
);

-- 인덱스
CREATE INDEX idx_ports_country ON ports(country);
CREATE INDEX idx_ports_region ON ports(region);
CREATE INDEX idx_ports_is_active ON ports(is_active);

-- ───────────────────────────────────────────────────────────────
-- 2. treasures (보물 - 크롤링된 상품)
-- ───────────────────────────────────────────────────────────────
CREATE TABLE treasures (
    id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
    port_id UUID NOT NULL REFERENCES ports(id) ON DELETE CASCADE,
    external_id VARCHAR(100),
    
    -- 기본 정보
    title VARCHAR(500) NOT NULL,
    description TEXT,
    short_description VARCHAR(300),
    
    -- 미디어
    image_urls JSONB DEFAULT '[]',
    thumbnail_url VARCHAR(500),
    video_url VARCHAR(500),
    
    -- 펀딩 정보
    original_url VARCHAR(1000) NOT NULL,
    funding_goal DECIMAL(15, 2),
    funding_current DECIMAL(15, 2),
    funding_percent INTEGER DEFAULT 0,
    backer_count INTEGER DEFAULT 0,
    currency VARCHAR(10) DEFAULT 'USD',
    
    -- 일정
    start_date TIMESTAMPTZ,
    end_date TIMESTAMPTZ,
    
    -- 분류
    category VARCHAR(100),
    subcategory VARCHAR(100),
    tags JSONB DEFAULT '[]',
    
    -- 메타
    creator_name VARCHAR(200),
    creator_avatar VARCHAR(500),
    
    -- 상태
    status VARCHAR(20) DEFAULT 'active', -- active, ended, cancelled
    is_featured BOOLEAN DEFAULT false,
    view_count INTEGER DEFAULT 0,
    wishlist_count INTEGER DEFAULT 0,
    
    -- 타임스탬프
    crawled_at TIMESTAMPTZ DEFAULT NOW(),
    created_at TIMESTAMPTZ DEFAULT NOW(),
    updated_at TIMESTAMPTZ DEFAULT NOW(),
    
    -- 유니크 제약
    UNIQUE(port_id, external_id)
);

-- 인덱스
CREATE INDEX idx_treasures_port_id ON treasures(port_id);
CREATE INDEX idx_treasures_category ON treasures(category);
CREATE INDEX idx_treasures_status ON treasures(status);
CREATE INDEX idx_treasures_end_date ON treasures(end_date);
CREATE INDEX idx_treasures_funding_percent ON treasures(funding_percent);
CREATE INDEX idx_treasures_crawled_at ON treasures(crawled_at);
CREATE INDEX idx_treasures_is_featured ON treasures(is_featured);

-- 전문 검색 인덱스
CREATE INDEX idx_treasures_title_search ON treasures USING gin(to_tsvector('english', title));

-- ───────────────────────────────────────────────────────────────
-- 3. captains (선장 - 사용자)
-- ───────────────────────────────────────────────────────────────
CREATE TABLE captains (
    id UUID PRIMARY KEY REFERENCES auth.users(id) ON DELETE CASCADE,
    
    -- 프로필
    nickname VARCHAR(50),
    email VARCHAR(255),
    avatar_url VARCHAR(500),
    
    -- 등급 시스템
    rank VARCHAR(20) DEFAULT 'apprentice', -- apprentice, navigator, captain, admiral, legend
    xp INTEGER DEFAULT 0,
    
    -- 리워드
    gold_coins INTEGER DEFAULT 0,
    
    -- 통계
    total_voyages INTEGER DEFAULT 0,      -- 총 방문 횟수
    total_discoveries INTEGER DEFAULT 0,  -- 발견한 보물 수
    total_trades INTEGER DEFAULT 0,       -- 구매 횟수
    ports_visited INTEGER DEFAULT 0,      -- 방문한 항구 수
    
    -- 설정
    notification_settings JSONB DEFAULT '{
        "new_treasure": true,
        "wishlist_update": true,
        "ending_soon": true,
        "marketing": false
    }',
    
    -- 타임스탬프
    last_active_at TIMESTAMPTZ DEFAULT NOW(),
    created_at TIMESTAMPTZ DEFAULT NOW(),
    updated_at TIMESTAMPTZ DEFAULT NOW()
);

-- ───────────────────────────────────────────────────────────────
-- 4. cargo (선적 화물 - 장바구니)
-- ───────────────────────────────────────────────────────────────
CREATE TABLE cargo (
    id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
    captain_id UUID NOT NULL REFERENCES captains(id) ON DELETE CASCADE,
    treasure_id UUID NOT NULL REFERENCES treasures(id) ON DELETE CASCADE,
    quantity INTEGER DEFAULT 1,
    reward_tier VARCHAR(100), -- 펀딩 보상 티어
    added_at TIMESTAMPTZ DEFAULT NOW(),
    
    UNIQUE(captain_id, treasure_id)
);

CREATE INDEX idx_cargo_captain_id ON cargo(captain_id);

-- ───────────────────────────────────────────────────────────────
-- 5. treasure_maps (보물 지도 - 위시리스트)
-- ───────────────────────────────────────────────────────────────
CREATE TABLE treasure_maps (
    id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
    captain_id UUID NOT NULL REFERENCES captains(id) ON DELETE CASCADE,
    treasure_id UUID NOT NULL REFERENCES treasures(id) ON DELETE CASCADE,
    note TEXT,
    created_at TIMESTAMPTZ DEFAULT NOW(),
    
    UNIQUE(captain_id, treasure_id)
);

CREATE INDEX idx_treasure_maps_captain_id ON treasure_maps(captain_id);
CREATE INDEX idx_treasure_maps_treasure_id ON treasure_maps(treasure_id);

-- ───────────────────────────────────────────────────────────────
-- 6. voyage_logs (항해 일지 - 조회 기록)
-- ───────────────────────────────────────────────────────────────
CREATE TABLE voyage_logs (
    id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
    captain_id UUID NOT NULL REFERENCES captains(id) ON DELETE CASCADE,
    treasure_id UUID NOT NULL REFERENCES treasures(id) ON DELETE CASCADE,
    viewed_at TIMESTAMPTZ DEFAULT NOW()
);

CREATE INDEX idx_voyage_logs_captain_id ON voyage_logs(captain_id);
CREATE INDEX idx_voyage_logs_viewed_at ON voyage_logs(viewed_at DESC);

-- ───────────────────────────────────────────────────────────────
-- 7. trades (교역 - 주문)
-- ───────────────────────────────────────────────────────────────
CREATE TABLE trades (
    id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
    captain_id UUID NOT NULL REFERENCES captains(id),
    
    -- 주문 정보
    order_number VARCHAR(50) UNIQUE NOT NULL,
    status VARCHAR(20) DEFAULT 'pending', -- pending, processing, shipped, delivered, cancelled
    
    -- 금액
    subtotal DECIMAL(15, 2) NOT NULL,
    shipping_fee DECIMAL(15, 2) DEFAULT 0,
    discount DECIMAL(15, 2) DEFAULT 0,
    total_amount DECIMAL(15, 2) NOT NULL,
    currency VARCHAR(10) DEFAULT 'KRW',
    
    -- 결제
    payment_method VARCHAR(50),
    payment_status VARCHAR(20) DEFAULT 'pending',
    paid_at TIMESTAMPTZ,
    
    -- 배송
    shipping_address JSONB,
    tracking_number VARCHAR(100),
    shipped_at TIMESTAMPTZ,
    delivered_at TIMESTAMPTZ,
    
    -- 적립
    gold_earned INTEGER DEFAULT 0,
    xp_earned INTEGER DEFAULT 0,
    
    -- 타임스탬프
    created_at TIMESTAMPTZ DEFAULT NOW(),
    updated_at TIMESTAMPTZ DEFAULT NOW()
);

CREATE INDEX idx_trades_captain_id ON trades(captain_id);
CREATE INDEX idx_trades_status ON trades(status);

-- ───────────────────────────────────────────────────────────────
-- 8. trade_items (교역 상품 - 주문 상세)
-- ───────────────────────────────────────────────────────────────
CREATE TABLE trade_items (
    id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
    trade_id UUID NOT NULL REFERENCES trades(id) ON DELETE CASCADE,
    treasure_id UUID NOT NULL REFERENCES treasures(id),
    
    quantity INTEGER DEFAULT 1,
    unit_price DECIMAL(15, 2) NOT NULL,
    reward_tier VARCHAR(100),
    
    created_at TIMESTAMPTZ DEFAULT NOW()
);

CREATE INDEX idx_trade_items_trade_id ON trade_items(trade_id);

-- ───────────────────────────────────────────────────────────────
-- 9. gold_transactions (금화 거래 내역)
-- ───────────────────────────────────────────────────────────────
CREATE TABLE gold_transactions (
    id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
    captain_id UUID NOT NULL REFERENCES captains(id),
    
    type VARCHAR(20) NOT NULL, -- earn, spend, expire
    amount INTEGER NOT NULL,
    balance_after INTEGER NOT NULL,
    
    description VARCHAR(200),
    reference_type VARCHAR(50), -- trade, review, event, etc.
    reference_id UUID,
    
    expires_at TIMESTAMPTZ,
    created_at TIMESTAMPTZ DEFAULT NOW()
);

CREATE INDEX idx_gold_transactions_captain_id ON gold_transactions(captain_id);

-- ───────────────────────────────────────────────────────────────
-- 10. permits (항해 허가증 - 쿠폰)
-- ───────────────────────────────────────────────────────────────
CREATE TABLE permits (
    id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
    captain_id UUID REFERENCES captains(id),
    
    code VARCHAR(50) UNIQUE NOT NULL,
    name VARCHAR(100) NOT NULL,
    description TEXT,
    
    discount_type VARCHAR(20) NOT NULL, -- percentage, fixed
    discount_value DECIMAL(10, 2) NOT NULL,
    min_order_amount DECIMAL(15, 2) DEFAULT 0,
    max_discount DECIMAL(15, 2),
    
    is_used BOOLEAN DEFAULT false,
    used_at TIMESTAMPTZ,
    trade_id UUID REFERENCES trades(id),
    
    valid_from TIMESTAMPTZ DEFAULT NOW(),
    valid_until TIMESTAMPTZ,
    
    created_at TIMESTAMPTZ DEFAULT NOW()
);

CREATE INDEX idx_permits_captain_id ON permits(captain_id);
CREATE INDEX idx_permits_code ON permits(code);

-- ───────────────────────────────────────────────────────────────
-- 11. reviews (항해사 평가 - 리뷰)
-- ───────────────────────────────────────────────────────────────
CREATE TABLE reviews (
    id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
    captain_id UUID NOT NULL REFERENCES captains(id),
    treasure_id UUID NOT NULL REFERENCES treasures(id),
    trade_id UUID REFERENCES trades(id),
    
    rating INTEGER NOT NULL CHECK (rating >= 1 AND rating <= 5),
    title VARCHAR(200),
    content TEXT,
    image_urls JSONB DEFAULT '[]',
    
    is_verified_purchase BOOLEAN DEFAULT false,
    helpful_count INTEGER DEFAULT 0,
    
    created_at TIMESTAMPTZ DEFAULT NOW(),
    updated_at TIMESTAMPTZ DEFAULT NOW(),
    
    UNIQUE(captain_id, treasure_id)
);

CREATE INDEX idx_reviews_treasure_id ON reviews(treasure_id);
CREATE INDEX idx_reviews_rating ON reviews(rating);

-- ───────────────────────────────────────────────────────────────
-- Views
-- ───────────────────────────────────────────────────────────────

-- 오늘 발견된 유물 (24시간 내 크롤링)
CREATE VIEW today_discoveries AS
SELECT t.*, p.name as port_name, p.country, p.logo_url as port_logo
FROM treasures t
JOIN ports p ON t.port_id = p.id
WHERE t.crawled_at >= NOW() - INTERVAL '24 hours'
  AND t.status = 'active'
ORDER BY t.crawled_at DESC;

-- 떠오르는 보물 (펀딩률 높은 순)
CREATE VIEW rising_treasures AS
SELECT t.*, p.name as port_name, p.country, p.logo_url as port_logo
FROM treasures t
JOIN ports p ON t.port_id = p.id
WHERE t.status = 'active'
  AND t.end_date > NOW()
ORDER BY t.funding_percent DESC
LIMIT 50;

-- 마감 임박 항해 (7일 이내 종료)
CREATE VIEW ending_soon AS
SELECT t.*, p.name as port_name, p.country, p.logo_url as port_logo,
       EXTRACT(DAY FROM (t.end_date - NOW())) as days_left
FROM treasures t
JOIN ports p ON t.port_id = p.id
WHERE t.status = 'active'
  AND t.end_date BETWEEN NOW() AND NOW() + INTERVAL '7 days'
ORDER BY t.end_date ASC;

-- ───────────────────────────────────────────────────────────────
-- Functions & Triggers
-- ───────────────────────────────────────────────────────────────

-- 위시리스트 카운트 업데이트 함수
CREATE OR REPLACE FUNCTION update_wishlist_count()
RETURNS TRIGGER AS $$
BEGIN
    IF TG_OP = 'INSERT' THEN
        UPDATE treasures SET wishlist_count = wishlist_count + 1 WHERE id = NEW.treasure_id;
    ELSIF TG_OP = 'DELETE' THEN
        UPDATE treasures SET wishlist_count = wishlist_count - 1 WHERE id = OLD.treasure_id;
    END IF;
    RETURN NULL;
END;
$$ LANGUAGE plpgsql;

CREATE TRIGGER trigger_update_wishlist_count
AFTER INSERT OR DELETE ON treasure_maps
FOR EACH ROW EXECUTE FUNCTION update_wishlist_count();

-- 조회수 업데이트 함수
CREATE OR REPLACE FUNCTION increment_view_count()
RETURNS TRIGGER AS $$
BEGIN
    UPDATE treasures SET view_count = view_count + 1 WHERE id = NEW.treasure_id;
    RETURN NEW;
END;
$$ LANGUAGE plpgsql;

CREATE TRIGGER trigger_increment_view_count
AFTER INSERT ON voyage_logs
FOR EACH ROW EXECUTE FUNCTION increment_view_count();

-- 선장 통계 업데이트 함수
CREATE OR REPLACE FUNCTION update_captain_stats()
RETURNS TRIGGER AS $$
BEGIN
    IF TG_TABLE_NAME = 'voyage_logs' THEN
        UPDATE captains SET 
            total_voyages = total_voyages + 1,
            last_active_at = NOW()
        WHERE id = NEW.captain_id;
    ELSIF TG_TABLE_NAME = 'trades' AND NEW.status = 'delivered' THEN
        UPDATE captains SET 
            total_trades = total_trades + 1,
            xp = xp + NEW.xp_earned,
            gold_coins = gold_coins + NEW.gold_earned
        WHERE id = NEW.captain_id;
    END IF;
    RETURN NEW;
END;
$$ LANGUAGE plpgsql;

CREATE TRIGGER trigger_update_captain_voyages
AFTER INSERT ON voyage_logs
FOR EACH ROW EXECUTE FUNCTION update_captain_stats();

-- ───────────────────────────────────────────────────────────────
-- Row Level Security (RLS)
-- ───────────────────────────────────────────────────────────────

ALTER TABLE captains ENABLE ROW LEVEL SECURITY;
ALTER TABLE cargo ENABLE ROW LEVEL SECURITY;
ALTER TABLE treasure_maps ENABLE ROW LEVEL SECURITY;
ALTER TABLE voyage_logs ENABLE ROW LEVEL SECURITY;
ALTER TABLE trades ENABLE ROW LEVEL SECURITY;
ALTER TABLE gold_transactions ENABLE ROW LEVEL SECURITY;
ALTER TABLE permits ENABLE ROW LEVEL SECURITY;
ALTER TABLE reviews ENABLE ROW LEVEL SECURITY;

-- 본인 데이터만 접근 가능
CREATE POLICY "Users can view own captain data" ON captains
    FOR SELECT USING (auth.uid() = id);

CREATE POLICY "Users can update own captain data" ON captains
    FOR UPDATE USING (auth.uid() = id);

CREATE POLICY "Users can manage own cargo" ON cargo
    FOR ALL USING (auth.uid() = captain_id);

CREATE POLICY "Users can manage own treasure maps" ON treasure_maps
    FOR ALL USING (auth.uid() = captain_id);

CREATE POLICY "Users can view own voyage logs" ON voyage_logs
    FOR SELECT USING (auth.uid() = captain_id);

CREATE POLICY "Users can insert own voyage logs" ON voyage_logs
    FOR INSERT WITH CHECK (auth.uid() = captain_id);

CREATE POLICY "Users can view own trades" ON trades
    FOR SELECT USING (auth.uid() = captain_id);

CREATE POLICY "Users can view own gold transactions" ON gold_transactions
    FOR SELECT USING (auth.uid() = captain_id);

CREATE POLICY "Users can view own permits" ON permits
    FOR SELECT USING (auth.uid() = captain_id);

-- 리뷰는 본인만 수정 가능, 모두 조회 가능
CREATE POLICY "Anyone can view reviews" ON reviews
    FOR SELECT USING (true);

CREATE POLICY "Users can manage own reviews" ON reviews
    FOR ALL USING (auth.uid() = captain_id);

-- 공개 데이터
CREATE POLICY "Anyone can view ports" ON ports
    FOR SELECT USING (true);

CREATE POLICY "Anyone can view treasures" ON treasures
    FOR SELECT USING (true);
```

---

## 7. 마케팅 및 수익화 전략

### 7.1 타겟 페르소나

#### Primary Target: 3040 남성 얼리어답터

```
┌─────────────────────────────────────────────────────────────┐
│                    페르소나: "탐험가 김선장"                   │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  👤 인구통계                                                │
│     - 나이: 32-42세 남성                                    │
│     - 직업: IT/스타트업/전문직                               │
│     - 소득: 연 5,000만원 이상                               │
│                                                             │
│  🎯 특성                                                    │
│     - 테크 얼리어답터: 신제품에 대한 높은 관심                 │
│     - 킥스타터/와디즈 경험자                                 │
│     - 정보 수집형: 유튜브, 블로그로 리서치                    │
│     - 투자 마인드: "좋은 제품에 투자한다"는 인식               │
│                                                             │
│  😤 Pain Points                                             │
│     - 여러 펀딩 사이트를 일일이 확인하기 번거로움              │
│     - 영어/일어 장벽으로 해외 제품 발견 어려움                 │
│     - 제품 신뢰성 판단 어려움                                │
│     - 배송/통관 불안                                        │
│                                                             │
│  ✨ 기대 가치                                                │
│     - 한 곳에서 글로벌 펀딩 제품 발견                        │
│     - 번역된 정보와 큐레이션                                 │
│     - 커뮤니티 검증                                         │
│     - 쉬운 구매 프로세스                                    │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### 7.2 마케팅 전략

#### Phase 1: 초기 사용자 확보 (런칭 ~ 3개월)

| 전략 | 실행 방안 | 목표 |
|------|----------|------|
| 얼리버드 탐험대 | 베타 테스터 500명 모집, 한정 "개척자" 칭호 부여 | 충성 사용자 확보 |
| 테크 유튜버 협업 | "이번 주 킥스타터 보물 TOP 5" 콘텐츠 | 인지도 확보 |
| 커뮤니티 공략 | 클리앙, 뽐뿌 IT 게시판 바이럴 | 얼리어답터 유입 |
| SNS 챌린지 | #나의첫보물발견 해시태그 캠페인 | UGC 생성 |

#### Phase 2: 성장 단계 (3개월 ~ 12개월)

| 전략 | 실행 방안 | 목표 |
|------|----------|------|
| SEO 콘텐츠 | "2024 킥스타터 추천", "해외 펀딩 구매법" 블로그 | 검색 유입 |
| 퍼포먼스 마케팅 | 관심 기반 타겟 광고 (Facebook, Google) | 신규 유입 |
| 레퍼럴 프로그램 | 친구 초대 시 양쪽 금화 지급 | 바이럴 성장 |
| 뉴스레터 | 주간 "항해 보고서" - 트렌드 제품 큐레이션 | 리텐션 |

#### 핵심 마케팅 카피

```
[인지 단계]
"전 세계 145개 펀딩 사이트를 일일이 확인하시나요?"

[관심 단계]  
"킥스타터, 인디고고, 마쿠아게...
이제 TradeWinds 하나로 발견하세요"

[전환 단계]
"남들보다 먼저 발견하는 사람이 트렌드를 만듭니다.
지금 항해를 시작하세요."
```

### 7.3 수익화 모델

#### 7.3.1 수익 구조

```
┌─────────────────────────────────────────────────────────────┐
│                    TradeWinds 수익 모델                      │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  1️⃣ 제휴 수수료 (Affiliate Revenue)          ████████ 40%   │
│     - 펀딩 참여 시 레퍼럴 수익 (1-8%)                        │
│     - 직접 구매 연결 수수료                                  │
│                                                             │
│  2️⃣ 프리미엄 멤버십 (Subscription)           ██████ 30%     │
│     - "제독 등급" 월 9,900원                                │
│     - 신규 보물 선공개 (24시간)                              │
│     - 독점 트렌드 리포트                                    │
│     - 무료 배송권 (월 1회)                                  │
│     - 프리미엄 고객 지원                                    │
│                                                             │
│  3️⃣ 광고 수익 (Advertising)                  ████ 15%       │
│     - 인피드 네이티브 광고                                  │
│     - 스폰서드 보물 (상단 노출)                              │
│     - 배너 광고 (프리미엄 회원 제외)                         │
│                                                             │
│  4️⃣ B2B 서비스 (Enterprise)                  ███ 15%        │
│     - 기업용 트렌드 리포트 구독                              │
│     - 제품 소싱 컨설팅                                      │
│     - 화이트라벨 솔루션                                     │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

#### 7.3.2 프리미엄 멤버십 상세

| 기능 | 일반 선장 | 제독 (Premium) |
|------|----------|----------------|
| 월 요금 | 무료 | ₩9,900 |
| 신규 보물 알림 | 일반 | 24시간 선공개 |
| 트렌드 리포트 | X | 주간 리포트 |
| 광고 | 있음 | 없음 |
| 배송비 할인 | X | 월 1회 무료 |
| 전용 항로 | X | 프리미엄 전용 상품 |
| 금화 보너스 | 1x | 1.5x |
| 고객 지원 | 일반 | 우선 응대 |

---

## 8. 개발 로드맵

### 8.1 개발 단계

```
═══════════════════════════════════════════════════════════════
                     개발 로드맵 타임라인
═══════════════════════════════════════════════════════════════

Phase 0: 기획 & 설계 (2주)
├── PRD 문서 작성
├── DB 스키마 설계
├── UI/UX 와이어프레임
└── 기술 스택 확정

Phase 1: MVP 개발 (8주)
├── Week 1-2: 프로젝트 셋업 & 인프라
│   ├── Flutter 프로젝트 초기화
│   ├── Supabase 설정 & 스키마 적용
│   └── CI/CD 파이프라인
│
├── Week 3-4: 인증 & 기본 UI
│   ├── 소셜 로그인 (Google, Apple, Kakao)
│   ├── 온보딩 플로우
│   └── 기본 네비게이션 구조
│
├── Week 5-6: 핵심 기능
│   ├── 홈 화면 (항해 본부)
│   ├── 상품 목록/상세 (보물 목록/감정서)
│   ├── 검색 & 필터 (정찰)
│   └── 세계 지도 탐험 (기본)
│
└── Week 7-8: 사용자 기능 & 테스트
    ├── 장바구니 (선적 화물)
    ├── 위시리스트 (보물 지도)
    ├── 마이페이지 (선장실)
    └── QA & 버그 픽스

Phase 2: 베타 런칭 (4주)
├── 크롤링 시스템 구축
├── 실제 데이터 수집 시작
├── 얼리버드 테스터 모집
└── 피드백 수집 & 개선

Phase 3: 정식 런칭 (4주)
├── 앱스토어 출시 (iOS, Android)
├── 결제 시스템 연동
├── 마케팅 캠페인 시작
└── 운영 안정화

Phase 4: 고도화 (지속)
├── AI 추천 시스템
├── 프리미엄 멤버십
├── 커뮤니티 기능
└── 글로벌 확장

═══════════════════════════════════════════════════════════════
```

### 8.2 MVP 기능 범위

#### Must Have (MVP 필수)

- [x] 사용자 인증 (소셜 로그인)
- [x] 상품 목록 조회
- [x] 상품 상세 페이지
- [x] 검색 & 기본 필터
- [x] 위시리스트
- [x] 세계 지도 (기본 버전)

#### Should Have (베타)

- [ ] 장바구니 & 주문
- [ ] 결제 연동
- [ ] 푸시 알림
- [ ] 리뷰 시스템
- [ ] 선장 등급 시스템

#### Could Have (정식 런칭 이후)

- [ ] AI 추천
- [ ] 프리미엄 멤버십
- [ ] 커뮤니티 기능
- [ ] 다국어 지원

---

## 부록

### A. 참고 자료

- [Flutter 공식 문서](https://flutter.dev/docs)
- [Supabase 공식 문서](https://supabase.com/docs)
- [flutter_map 패키지](https://pub.dev/packages/flutter_map)
- [flutter_bloc 패키지](https://pub.dev/packages/flutter_bloc)

### B. 용어 사전

| 용어 | 정의 |
|------|------|
| 크라우드펀딩 | 대중으로부터 자금을 모으는 방식 |
| MVP | Minimum Viable Product, 최소 기능 제품 |
| BLoC | Business Logic Component, Flutter 상태관리 패턴 |
| RLS | Row Level Security, Supabase 행 단위 보안 |

### C. 변경 이력

| 버전 | 날짜 | 변경 내용 | 작성자 |
|------|------|----------|--------|
| 1.0.0 | 2024-12 | 최초 작성 | Product Team |

---

*© 2024 TradeWinds. All rights reserved.*

