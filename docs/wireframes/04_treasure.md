# 보물 목록/감정서 화면 와이어프레임

> **화면명:** 보물 목록 (Treasure List) & 보물 감정서 (Treasure Detail)  
> **파일 위치:** `lib/presentation/pages/treasure/`

---

## Part A: 보물 목록 (Treasure List)

### 1. 전체 레이아웃

```
┌─────────────────────────────────────┐
│ ┌─────────────────────────────────┐ │
│ │ ← Kickstarter 보물함      🔍 ⚙️ │ │  ← AppBar
│ └─────────────────────────────────┘ │
├─────────────────────────────────────┤
│  ┌─────────────────────────────┐   │
│  │ 🔍 이 항구에서 검색...        │   │  ← 검색 바
│  └─────────────────────────────┘   │
│                                     │
│ [전체] [테크] [디자인] [게임] [생활] │  ← 카테고리 탭
│                                     │
│  📊 1,234개 보물 | 정렬: 인기순 ▼   │  ← 결과 요약 & 정렬
│                                     │
│  ┌─────────┐ ┌─────────┐          │
│  │         │ │         │          │  ← 보물 그리드
│  │  보물1   │ │  보물2   │          │
│  │         │ │         │          │
│  └─────────┘ └─────────┘          │
│  ┌─────────┐ ┌─────────┐          │
│  │         │ │         │          │
│  │  보물3   │ │  보물4   │          │
│  │         │ │         │          │
│  └─────────┘ └─────────┘          │
│  ┌─────────┐ ┌─────────┐          │
│  │         │ │         │          │
│  │  보물5   │ │  보물6   │          │
│  │         │ │         │          │
│  └─────────┘ └─────────┘          │
│                                     │
│        [더 많은 보물 탐색]           │  ← 페이지네이션
│                                     │
├─────────────────────────────────────┤
│ [🏠] [🗺️] [🧭] [📦] [👤]          │
└─────────────────────────────────────┘
```

---

### 2. AppBar 상세

```
┌─────────────────────────────────────────────────┐
│ ←  [로고] Kickstarter 보물함            🔍  ⚙️  │
└─────────────────────────────────────────────────┘
     뒤로   항구로고   항구명            검색  필터
```

```dart
AppBar(
  leading: BackButton(),
  title: Row(
    children: [
      CircleAvatar(
        backgroundImage: NetworkImage(port.logoUrl),
        radius: 16,
      ),
      SizedBox(width: 8),
      Column(
        crossAxisAlignment: CrossAxisAlignment.start,
        children: [
          Text('Kickstarter', style: AppTypography.bodyLarge),
          Text('보물함', style: TextStyle(color: AppColors.textTertiary, fontSize: 12)),
        ],
      ),
    ],
  ),
  actions: [
    IconButton(icon: Icon(Icons.search), onPressed: () {}),
    IconButton(icon: Icon(Icons.filter_list), onPressed: () {}),
  ],
)
```

---

### 3. 카테고리 탭

```
┌─────────────────────────────────────────────────────────┐
│ [전체] [테크] [디자인] [게임] [생활] [패션] [푸드]  ──→ │
└─────────────────────────────────────────────────────────┘
```

```dart
SingleChildScrollView(
  scrollDirection: Axis.horizontal,
  child: Row(
    children: categories.map((cat) => CategoryChip(
      label: cat.name,
      isSelected: cat == selectedCategory,
      onTap: () => selectCategory(cat),
    )).toList(),
  ),
)
```

---

### 4. 정렬 & 필터

### 4.1 정렬 옵션

```
┌─────────────────────────────────────────────────┐
│ 📊 1,234개 보물                  정렬: 인기순 ▼  │
└─────────────────────────────────────────────────┘
                                        ↓
                           ┌───────────────────┐
                           │ ● 인기순           │
                           │ ○ 최신순           │
                           │ ○ 펀딩률 높은순    │
                           │ ○ 마감 임박순      │
                           │ ○ 가격 낮은순      │
                           │ ○ 가격 높은순      │
                           └───────────────────┘
```

### 4.2 필터 바텀시트

```
┌─────────────────────────────────────┐
│ ─────────────                       │
│                                     │
│  ⚙️ 필터 설정                        │
│                                     │
│  💰 가격대                           │
│  ├─────────────────●───────────┤   │
│  $0            $250          $500+  │
│                                     │
│  📊 펀딩 상태                        │
│  [● 진행중] [○ 성공] [○ 마감]        │
│                                     │
│  ⏰ 마감까지                         │
│  [○ 전체] [● 7일 이내] [○ 30일 이내] │
│                                     │
│  🏷️ 태그                            │
│  [스마트홈] [웨어러블] [친환경] ...    │
│                                     │
│  ┌──────────────┐ ┌──────────────┐ │
│  │    초기화     │ │   적용하기    │ │
│  └──────────────┘ └──────────────┘ │
│                                     │
└─────────────────────────────────────┘
```

---

### 5. 보물 카드 (그리드)

```
┌─────────────────────────┐
│                         │
│   [제품 이미지]          │  (width) x 140
│                         │
│    ♡                    │  ← 찜 버튼 (오버레이)
├─────────────────────────┤
│                         │
│ 🏷️ 테크 | ⭐ 4.8        │  ← 카테고리, 평점
│                         │
│ Revolutionary Smart     │  ← 제목 (2줄 max)
│ Watch with AI Assis...  │
│                         │
│ 💰 $149                 │  ← 가격
│    (~₩199,000)          │  ← 원화 환산
│                         │
│ ████████████░░░░ 285%   │  ← 펀딩 진행률
│                         │
│ ⏰ D-15 | 👥 2,847명     │  ← 남은 기간, 후원자
│                         │
└─────────────────────────┘
```

```dart
class TreasureCard extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Card(
      elevation: 2,
      shape: RoundedRectangleBorder(borderRadius: BorderRadius.circular(12)),
      child: Column(
        crossAxisAlignment: CrossAxisAlignment.start,
        children: [
          // 이미지 영역
          Stack(
            children: [
              ClipRRect(
                borderRadius: BorderRadius.vertical(top: Radius.circular(12)),
                child: Image.network(
                  treasure.imageUrl,
                  height: 140,
                  width: double.infinity,
                  fit: BoxFit.cover,
                ),
              ),
              Positioned(
                top: 8,
                right: 8,
                child: WishlistButton(treasureId: treasure.id),
              ),
            ],
          ),
          // 정보 영역
          Padding(
            padding: EdgeInsets.all(12),
            child: Column(
              crossAxisAlignment: CrossAxisAlignment.start,
              children: [
                // 카테고리 & 평점
                Row(
                  children: [
                    CategoryBadge(category: treasure.category),
                    Spacer(),
                    if (treasure.rating != null)
                      Row(
                        children: [
                          Icon(Icons.star, size: 14, color: AppColors.gold),
                          Text(' ${treasure.rating}'),
                        ],
                      ),
                  ],
                ),
                SizedBox(height: 8),
                // 제목
                Text(
                  treasure.title,
                  style: AppTypography.bodyMedium,
                  maxLines: 2,
                  overflow: TextOverflow.ellipsis,
                ),
                SizedBox(height: 8),
                // 가격
                PriceText(
                  price: treasure.price,
                  currency: treasure.currency,
                ),
                SizedBox(height: 8),
                // 펀딩 진행률
                FundingProgressBar(
                  percentage: treasure.fundingPercentage,
                ),
                SizedBox(height: 4),
                // 남은 기간 & 후원자
                Row(
                  children: [
                    Text('⏰ D-${treasure.daysLeft}'),
                    Spacer(),
                    Text('👥 ${treasure.backerCount}명'),
                  ],
                ),
              ],
            ),
          ),
        ],
      ),
    );
  }
}
```

---

### 6. 뷰 모드 전환

```
┌──────────────────────────┐
│  [▤ 그리드] [☰ 리스트]   │
└──────────────────────────┘
```

#### 리스트 뷰 카드

```
┌─────────────────────────────────────────────────┐
│ ┌─────┐                                         │
│ │     │  Smart Watch with AI Assistant          │
│ │이미지│  🏷️ 테크 | Kickstarter                  │
│ │     │  💰 $149 (~₩199K)                       │
│ │     │  ████████░░ 285% | D-15 | 2.8K명        │
│ └─────┘                                    ♡    │
└─────────────────────────────────────────────────┘
```

---

## Part B: 보물 감정서 (Treasure Detail)

### 1. 전체 레이아웃

```
┌─────────────────────────────────────┐
│ ← 보물 감정서                 ♡  ↗  │  ← AppBar
├─────────────────────────────────────┤
│                                     │
│ ┌─────────────────────────────────┐ │
│ │                                 │ │
│ │      [메인 제품 이미지]          │ │  ← 이미지 캐러셀
│ │        (스와이프 가능)           │ │
│ │                                 │ │
│ └─────────────────────────────────┘ │
│           ○ ● ○ ○ ○                │  ← 인디케이터
│                                     │
│  🏴 Kickstarter | 🇺🇸 미국          │  ← 항구 정보
│                                     │
│  Revolutionary Smart Watch         │  ← 제목
│  with AI Assistant                 │
│                                     │
│ ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ │
│                                     │
│  💰 펀딩 정보                        │
│  ┌─────────────────────────────┐   │
│  │  현재 금액    │  목표 금액     │   │
│  │  $284,750    │  $100,000    │   │
│  │              │              │   │
│  │  █████████████████████ 285% │   │
│  └─────────────────────────────┘   │
│                                     │
│  ⏰ D-15 남음  |  👥 2,847명 참여    │
│                                     │
│ ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ │
│                                     │
│  💎 리워드 옵션                      │
│  ┌─────────────────────────────┐   │
│  │  Early Bird Special         │   │
│  │  $149 (~₩199,000)           │   │
│  │  ✓ 본품 1개                  │   │
│  │  ✓ 전용 충전기               │   │
│  │  📦 2024년 6월 배송 예정      │   │
│  │                      [선택] │   │
│  └─────────────────────────────┘   │
│                                     │
│  ┌─────────────────────────────┐   │
│  │  Standard Pack              │   │
│  │  $199 (~₩265,000)           │   │
│  │  ✓ 본품 1개                  │   │
│  │  ✓ 전용 충전기               │   │
│  │  ✓ 추가 스트랩 2개           │   │
│  │  📦 2024년 6월 배송 예정      │   │
│  │                      [선택] │   │
│  └─────────────────────────────┘   │
│                                     │
│ ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ │
│                                     │
│  📜 보물 설명서                      │  ← 탭 영역
│  [상세정보] [제작자] [후기] [FAQ]     │
│                                     │
│  (제품 상세 설명 콘텐츠...)           │
│                                     │
│                                     │
│                                     │
├─────────────────────────────────────┤
│ ┌──────────────┐ ┌──────────────┐  │  ← 하단 고정 버튼
│ │  🗺️ 지도에 담기 │ │  📦 선적하기   │  │
│ └──────────────┘ └──────────────┘  │
└─────────────────────────────────────┘
```

---

### 2. 이미지 캐러셀

```dart
PageView.builder(
  itemCount: treasure.images.length,
  onPageChanged: (index) => setState(() => currentImage = index),
  itemBuilder: (context, index) => GestureDetector(
    onTap: () => openImageViewer(index),
    child: Image.network(
      treasure.images[index],
      fit: BoxFit.cover,
    ),
  ),
)
```

- **높이:** 화면 너비의 80% (1:0.8 비율)
- **기능:** 좌우 스와이프, 탭 시 전체화면 뷰어
- **인디케이터:** 이미지 개수만큼 점 표시

---

### 3. 펀딩 정보 카드

```
┌───────────────────────────────────────────────┐
│                                               │
│   현재 금액                    목표 금액        │
│   $284,750                   $100,000        │
│   (₩380,000,000)             (₩133,000,000)  │
│                                               │
│   ████████████████████████████████████  285% │
│                                               │
│   ┌─────────┐  ┌─────────┐  ┌─────────┐     │
│   │  D-15   │  │  2,847  │  │  $100   │     │
│   │ 남은일수 │  │  후원자  │  │ 평균후원 │     │
│   └─────────┘  └─────────┘  └─────────┘     │
│                                               │
└───────────────────────────────────────────────┘
```

```dart
Container(
  padding: EdgeInsets.all(16),
  decoration: BoxDecoration(
    color: AppColors.parchment,
    borderRadius: BorderRadius.circular(16),
    border: Border.all(color: AppColors.gold.withOpacity(0.3)),
  ),
  child: Column(
    children: [
      Row(
        mainAxisAlignment: MainAxisAlignment.spaceBetween,
        children: [
          FundingAmount(
            title: '현재 금액',
            amount: treasure.currentAmount,
            currency: treasure.currency,
          ),
          FundingAmount(
            title: '목표 금액',
            amount: treasure.goalAmount,
            currency: treasure.currency,
          ),
        ],
      ),
      SizedBox(height: 16),
      FundingProgressBar(
        percentage: treasure.fundingPercentage,
        height: 12,
        showLabel: true,
      ),
      SizedBox(height: 16),
      Row(
        mainAxisAlignment: MainAxisAlignment.spaceEvenly,
        children: [
          StatItem(icon: '⏰', value: 'D-${treasure.daysLeft}', label: '남은일수'),
          StatItem(icon: '👥', value: '${treasure.backerCount}', label: '후원자'),
          StatItem(icon: '💵', value: '\$${treasure.avgPledge}', label: '평균후원'),
        ],
      ),
    ],
  ),
)
```

---

### 4. 리워드 카드

```
┌─────────────────────────────────────────────────┐
│  🏅 Early Bird Special              ⭐ 인기     │
│                                                 │
│  💰 $149                                        │
│     (~₩199,000)                                 │
│                                                 │
│  포함 항목:                                      │
│  ✓ 스마트워치 본품 1개                           │
│  ✓ 전용 무선 충전기                              │
│  ✓ 사용 설명서 (한국어 포함)                      │
│                                                 │
│  📦 배송 예정: 2024년 6월                        │
│  🌍 한국 배송 가능                               │
│  📊 1,234명 선택                                │
│                                                 │
│         ┌────────────────────────┐             │
│         │    이 리워드 선택하기    │             │
│         └────────────────────────┘             │
└─────────────────────────────────────────────────┘
```

---

### 5. 상세 정보 탭

```dart
DefaultTabController(
  length: 4,
  child: Column(
    children: [
      TabBar(
        labelColor: AppColors.primary,
        unselectedLabelColor: AppColors.textTertiary,
        indicatorColor: AppColors.gold,
        tabs: [
          Tab(text: '상세정보'),
          Tab(text: '제작자'),
          Tab(text: '후기'),
          Tab(text: 'FAQ'),
        ],
      ),
      TabBarView(
        children: [
          ProductDescriptionTab(),
          CreatorInfoTab(),
          ReviewsTab(),
          FAQTab(),
        ],
      ),
    ],
  ),
)
```

---

### 6. 하단 액션 버튼

```
┌─────────────────────────────────────────────────┐
│                                                 │
│   ┌─────────────────┐  ┌─────────────────┐     │
│   │   🗺️ 지도에 담기  │  │   📦 선적하기    │     │
│   │    (찜하기)      │  │   (장바구니)     │     │
│   └─────────────────┘  └─────────────────┘     │
│                                                 │
└─────────────────────────────────────────────────┘
```

```dart
Container(
  padding: EdgeInsets.all(16),
  decoration: BoxDecoration(
    color: Colors.white,
    boxShadow: [
      BoxShadow(
        color: Colors.black12,
        blurRadius: 8,
        offset: Offset(0, -2),
      ),
    ],
  ),
  child: Row(
    children: [
      Expanded(
        child: OutlinedButton.icon(
          onPressed: () => addToWishlist(treasure.id),
          icon: Icon(Icons.map),
          label: Text('지도에 담기'),
          style: OutlinedButton.styleFrom(
            padding: EdgeInsets.symmetric(vertical: 16),
            side: BorderSide(color: AppColors.gold),
            foregroundColor: AppColors.gold,
          ),
        ),
      ),
      SizedBox(width: 12),
      Expanded(
        child: ElevatedButton.icon(
          onPressed: () => addToCart(treasure.id, selectedReward),
          icon: Icon(Icons.inventory_2),
          label: Text('선적하기'),
          style: ElevatedButton.styleFrom(
            padding: EdgeInsets.symmetric(vertical: 16),
            backgroundColor: AppColors.gold,
            foregroundColor: AppColors.textOnGold,
          ),
        ),
      ),
    ],
  ),
)
```

---

### 7. 공유 기능

```dart
IconButton(
  icon: Icon(Icons.share),
  onPressed: () {
    Share.share(
      '${treasure.title}\n${treasure.originalUrl}',
      subject: 'TradeWinds에서 발견한 보물!',
    );
  },
)
```

---

### 8. 상태 관리

```dart
class TreasureDetailState {
  final Treasure? treasure;
  final List<Reward> rewards;
  final int selectedRewardIndex;
  final bool isLoading;
  final bool isInWishlist;
  final String? error;
}

abstract class TreasureDetailEvent {}
class LoadTreasureDetail extends TreasureDetailEvent { final String id; }
class SelectReward extends TreasureDetailEvent { final int index; }
class ToggleWishlist extends TreasureDetailEvent {}
class AddToCart extends TreasureDetailEvent {}
```

